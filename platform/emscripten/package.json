{
  "name": "eduke32-net",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser multiplayer TRANSPORT for the EDuke32-WASM port: WebRTC data channels signaled over Nostr relays, plus GRP discovery/gating/transfer. Bundles to a single self-contained eduke32-net.js that runs beside eduke32.js. The netcode (netduke32) lives in the C engine; this is the wire only.",
  "scripts": {
    "build:net": "node esbuild.config.mjs",
    "watch:net": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "nostr-tools": "^2.23.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.8.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
